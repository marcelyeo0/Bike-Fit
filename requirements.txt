# Versions figées : mediapipe 0.10.21 est la dernière version qui embarque
# l'API « solutions » (modèle inclus dans le paquet, crucial pour PyInstaller).
# Les versions plus récentes (0.10.3x) imposent l'API Tasks + téléchargement
# du modèle. mediapipe 0.10.21 exige numpy<2, d'où opencv-python 4.10
# (opencv 5.x exige numpy>=2). Cf. .claude/notes.md, entrée du 2026-07-10.
mediapipe==0.10.21
opencv-python==4.10.0.84
numpy>=1.26,<2
PySide6>=6.7
pytest>=8.2
